import { types } from "../redux/types"

export const loginAction =(token)=>{
type:types.login,
payload:{
    <template tdLoading="loadingTemplateRef" tdLoadingType="circular" tdLoadingMode="indeterminate" tdLoadingStrategy="replace" tdLoadingColor="primary">
        
    </template>
}
    
    }